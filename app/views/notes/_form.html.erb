<%= form_for(@task_note) do |f| %>
  <% if @task_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_note.errors.count, "error") %> prohibited this task_note from being saved:</h2>

      <ul>
      <% @task_note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.select :user_id, User.all_for_select %>
  </div>

    <div class="field">
      <%= f.label :task_id %><br />
      <%= f.select :task_id, Task.all_for_select %>
    </div>

  <div class="field">
    <%= f.label :owner %><br />
    <%= f.text_field :owner %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
